#include<stdio.h>
int main(){
    // int phone = 120;
    // int tv = 188;
    // printf("修改前：\n手机库存为%d\n电视库存为%d\n",phone,tv);
    // int x;
    // x = phone;
    // phone = tv;
    // tv = x;
    // printf("修改后：\n手机库存为%d\n电视库存为%d\n",phone,tv);


    int a = 5;
    int b = 3;
    printf("按位与的结果为%d\n",a & b);
    printf("按位或的结果为%d\n",a | b);
    printf("按位异或的结果为%d\n",!a ^ b);
    a = -8;
    b = 12;
    printf("按位与的结果为%d\n",a&b);
    printf("按位或的结果为%d\n",a|b);
    printf("按位异或的结果为%d\n",a^b);


}